import { Command } from "@oclif/command";
export default class Test extends Command {
    static description: string;
    static examples: string[];
    static flags: {
        "no-fail-fast": import("@oclif/parser/lib/flags").IBooleanFlag<boolean>;
    };
    static args: {
        name: string;
        required: boolean;
    }[];
    run(): Promise<void>;
}
